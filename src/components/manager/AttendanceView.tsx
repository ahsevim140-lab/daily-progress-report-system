import React, { useMemo, useState } from 'react';
import { AttendanceStatus, BackendData } from '../../types';
import { todayLocalYMD } from '../../utils';
import { upsertAttendance } from '../../services/supabaseService';
import { CalendarDays, Printer, Save } from 'lucide-react';

const labels: Record<AttendanceStatus, string> = { present: 'Present', late: 'Late', day_off: 'Day off', hours_off: 'Hours off', absent: 'Absent' };
export const AttendanceView: React.FC<{ backendData: BackendData; onRefreshData: () => void }> = ({ backendData, onRefreshData }) => {
  const today = todayLocalYMD(); const [date, setDate] = useState(today); const [department, setDepartment] = useState(''); const [statusFilter, setStatusFilter] = useState(''); const [saving, setSaving] = useState<string | null>(null);
  const rows = useMemo(() => backendData.employees.filter((e) => !department || e.department === department).map((employee) => backendData.attendance.find((r) => r.employee_id === employee.id && r.attendance_date === date) || ({ employee_id: employee.id, employee_name: employee.name, department: employee.department, attendance_date: date, entrance_time: '', status: 'present' as AttendanceStatus, hours_off: 0, note: '' })), [backendData, date, department]);
  const filtered = statusFilter ? rows.filter((r) => r.status === statusFilter) : rows;
  const save = async (row: any) => { setSaving(row.employee_id); try { await upsertAttendance({ employee_id: row.employee_id, employee_name: row.employee_name, department: row.department, attendance_date: date, entrance_time: row.entrance_time || null, status: row.status, hours_off: Number(row.hours_off) || 0, note: row.note || null }); onRefreshData(); } finally { setSaving(null); } };
  return <div className="space-y-4 print:text-black"><div className="flex flex-wrap items-end gap-3 bg-[#FBF8EF] border border-[#DED2AC] rounded-2xl p-4"><label className="text-xs">Date<input type="date" value={date} onChange={(e) => setDate(e.target.value)} className="block mt-1 bg-white border border-[#DED2AC] rounded-lg px-2 py-1.5 text-xs" /></label><label className="text-xs">Department<select value={department} onChange={(e) => setDepartment(e.target.value)} className="block mt-1 bg-white border border-[#DED2AC] rounded-lg px-2 py-1.5 text-xs"><option value="">All departments</option>{backendData.departments.map((d) => <option key={d}>{d}</option>)}</select></label><label className="text-xs">Status<select value={statusFilter} onChange={(e) => setStatusFilter(e.target.value)} className="block mt-1 bg-white border border-[#DED2AC] rounded-lg px-2 py-1.5 text-xs"><option value="">All statuses</option>{Object.entries(labels).map(([v, l]) => <option key={v} value={v}>{l}</option>)}</select></label><button onClick={() => window.print()} className="ml-auto px-3 py-2 bg-[#3B4636] text-white rounded-lg text-xs flex items-center gap-2"><Printer className="w-4 h-4" />Print filtered report</button></div><div className="bg-[#FBF8EF] border border-[#DED2AC] rounded-2xl p-4 overflow-x-auto"><div className="flex items-center gap-2 font-bold text-[#3B4636] mb-3"><CalendarDays className="w-4 h-4 text-[#B89B5E]" />Attendance for {date}</div><table className="w-full text-xs"><thead><tr className="text-right border-b border-[#DED2AC]"><th className="p-2">Employee</th><th className="p-2">Department</th><th className="p-2">Entrance</th><th className="p-2">Status</th><th className="p-2">Hours off</th><th className="p-2">Note</th><th /></tr></thead><tbody>{filtered.map((row: any) => <tr key={row.employee_id} className="border-b border-[#DED2AC]/70"><td className="p-2 font-medium">{row.employee_name}</td><td className="p-2">{row.department}</td><td className="p-2"><input type="time" defaultValue={row.entrance_time || ''} onChange={(e) => { row.entrance_time = e.target.value; }} className="bg-white border border-[#DED2AC] rounded px-2 py-1" /></td><td className="p-2"><select defaultValue={row.status} onChange={(e) => { row.status = e.target.value; }} className="bg-white border border-[#DED2AC] rounded px-2 py-1">{Object.entries(labels).map(([v, l]) => <option key={v} value={v}>{l}</option>)}</select></td><td className="p-2"><input type="number" min="0" step="0.5" defaultValue={row.hours_off || 0} onChange={(e) => { row.hours_off = e.target.value; }} className="w-16 bg-white border border-[#DED2AC] rounded px-2 py-1" /></td><td className="p-2"><input defaultValue={row.note || ''} onChange={(e) => { row.note = e.target.value; }} className="w-32 bg-white border border-[#DED2AC] rounded px-2 py-1" /></td><td className="p-2"><button disabled={saving === row.employee_id} onClick={() => save(row)} className="text-[#3B4636]"><Save className="w-4 h-4" /></button></td></tr>)}</tbody></table>{!filtered.length && <div className="text-center text-xs text-stone-500 py-8">No employees match the filters.</div>}</div></div>;
};
